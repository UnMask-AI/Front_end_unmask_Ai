import type { ReactNode } from "react";

export const metadata = {
  title: "Status — UnMask AI",
};

export default function StatusLayout({
  children,
}: {
  children: ReactNode;
}) {
  return children;
}
